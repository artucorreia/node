services:
  database:
    image: postgres:17-alpine
    container_name: database_container
    env_file:
      - ../.env
    ports:
      - "2345:5432"
    restart: always
    networks:
      - app



networks:
  app: